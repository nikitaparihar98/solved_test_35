#include<iostream>
using namespace std;
int main(){
    int n;
    cin >> n;
    int nums[n];
    for(int i=0 ; i< n ; i++){
        cin >> nums[i];
    }
    int Largest = INT_MIN;
    for(int i=0 ; i< n ; i++){
        Largest = max(nums[i],Largest);
    }
    cout<< Largest << endl;
}
