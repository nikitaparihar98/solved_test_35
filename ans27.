#include <iostream>
using namespace std;

bool searchMatrix(int matrix[][3], int n, int m, int target) {
    int low = 0, high = n * m - 1;
    while (low <= high) {
        int mid = (low + high) / 2;
        int val = matrix[mid / m][mid % m];
        if (val == target)
            return true;
        else if (val < target)
            low = mid + 1;
        else
            high = mid - 1;
    }
    return false;
}

int main() {
    int n = 3, m = 3;
    int matrix[3][3];
    for (int i = 0; i < n; i++)
        for (int j = 0; j < m; j++)
            cin >> matrix[i][j];
    int target;
    cin >> target;
    if (searchMatrix(matrix, n, m, target))
        cout << "True";
    else
        cout << "False";
    return 0;
}
